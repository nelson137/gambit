{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cargo build",
            "type": "cargo",
            "command": "build",
            "group": "build",
            "problemMatcher": [
                "$rustc"
            ]
        }
    ]
}
